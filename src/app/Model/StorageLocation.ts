export class StorageLocation{
    rackid: number;
    locationno: string

    constructor(rackid: number,
        locationno: string){
            this.rackid=rackid
            this.locationno=locationno

    }
    
}