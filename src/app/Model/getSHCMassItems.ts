export class getSHCMassItems{
        itemid: number;
        itemcode: string;

        constructor(itemid: number,
            itemcode: string){
                this.itemid=itemid;
                this.itemcode= itemcode;

        }
}